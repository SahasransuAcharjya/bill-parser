# 1. Force Python 3.11 (Fixes 'No module named distutils')
[variables]
PYTHON_VERSION = "3.11"

# 2. Install System Libraries (Fixes 'libGL.so.1' and Tesseract errors)
[phases.setup]
nixPkgs = ["tesseract", "libgl1", "libglib2.0-0"]